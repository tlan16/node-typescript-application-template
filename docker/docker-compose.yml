---
version: '3.9'
services:
  test:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: 'pnpm run test'
  build:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: 'pnpm run build'
  lint:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: 'pnpm run lint'
